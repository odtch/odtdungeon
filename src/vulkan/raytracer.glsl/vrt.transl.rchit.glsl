#version 460
#extension GL_GOOGLE_include_directive : require
#include "vrt.common.glsl.h"
#include "vrt.closesthit.glsl.h"

void main(){
	/*
	HitInfo hitInfo = getObjectHitInfo();
//	if( hitInfo.color.a < 0.03 ){
//		pld.hasHit = false;
//		return;
//	}
	pld.hasHit = true;
	pld.instance_index = globals.layers[ pld.layer ].first_instance_index + gl_InstanceID;
	pld.material_index = instances[ pld.instance_index ].materialIndex;
	pld.hitColor = hitInfo.color; // vec4( 0.5f, 0.5f, 0.5f, 1 ) * 2;
	pld.hitPosition = hitInfo.worldPosition;
	pld.instance_index = hitInfo.instance_index;
	*/
}

